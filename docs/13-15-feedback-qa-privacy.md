# الأمر 13: نظام التحليل والتحسين المستمر (AI Feedback Loop)

> **المرجع:** أمر التعلم #13
> **المبدأ:** النظام يتعلم من كل مستخدم — بخصوصية صارمة.

---

## آلية جمع الملاحظات الصامتة:

### 1. تتبع تعديلات المستخدم (Silent Telemetry)

```typescript
interface UserModificationTracker {
  // ما نتتبعه:
  tracked_events: {
    color_change: {
      from: string;   // اللون الذي اختاره AI
      to: string;     // اللون الذي اختاره المستخدم
      component: string; // أي مكون تم تغييره
      frequency: number; // كم مرة حدث هذا النمط
    };
    text_edit: {
      component: string;
      original_length: number;
      new_length: number;
      was_ai_generated: boolean;
      // ❌ لا نخزن: النص الفعلي (خصوصية)
    };
    component_reorder: {
      moved_component: string;
      from_position: number;
      to_position: number;
    };
    component_delete: {
      deleted_component: string;
      was_ai_suggested: boolean;
    };
    component_add: {
      added_component: string;
      position: number;
    };
  };

  // ❌ ما لا نتتبعه أبدًا:
  never_tracked: [
    'محتوى النصوص الفعلي',
    'بيانات العملاء الشخصية',
    'معلومات الاتصال',
    'كلمات المرور',
    'بيانات الدفع',
  ];
}
```

### 2. تحليل أسئلة الدعم

```typescript
interface SupportAnalytics {
  // تصنيف تلقائي للأسئلة:
  categories: {
    'how-to-add-page': { count: 0, trend: 'increasing' },
    'how-to-change-color': { count: 0, trend: 'stable' },
    'how-to-connect-domain': { count: 0, trend: 'decreasing' },
    'bug-report': { count: 0, trend: 'stable' },
    'feature-request': { count: 0, trend: 'increasing' },
  };

  // إجراءات تلقائية بناءً على التحليل:
  auto_actions: {
    if_category_count_exceeds: (category: string, threshold: number) => {
      // إذا سأل 100+ مستخدم "كيف أضيف صفحة؟"
      // → تحسين تجربة إضافة الصفحات تلقائيًا
      // → إضافة tooltip / onboarding step
    };
  };
}
```

### 3. تحسين برومبتات التوليد

```typescript
interface PromptOptimizer {
  // مثال: إذا 80% من المستخدمين يغيرون لون الخلفية إلى أفتح
  learning_rule: {
    observation: '80% of users lighten the AI-chosen background',
    action: 'تعديل Default Background Lightness من 50% إلى 70%',
    implementation: (currentPrompt: string) => {
      return currentPrompt.replace(
        'background_lightness: moderate',
        'background_lightness: light (users prefer lighter backgrounds by 80%)'
      );
    };
  };

  // مثال: إذا معظم المستخدمين يحذفون قسم FAQ
  learning_rule_2: {
    observation: '60% of restaurant sites delete the FAQ section',
    action: 'لا تضيف FAQ تلقائيًا لمواقع المطاعم',
    implementation: (businessType: string) => {
      if (businessType === 'restaurant') {
        return { include_faq: false };
      }
    };
  };
}
```

### 4. حماية الخصوصية الصارمة

```typescript
const PRIVACY_RULES = {
  // كل البيانات مُجمّعة وإحصائية فقط
  aggregation: 'statistical_only',

  // لا ربط بين بيانات التتبع وهوية المستخدم
  anonymization: 'full',

  // البيانات تُحذف بعد 90 يومًا
  retention_days: 90,

  // المستخدم يمكنه إيقاف التتبع
  opt_out: {
    available: true,
    location: 'الإعدادات → الخصوصية → إيقاف مشاركة بيانات الاستخدام',
    effect: 'فوري — لن يتم تتبع أي بيانات',
  },

  // لا مشاركة مع أطراف ثالثة
  third_party_sharing: false,

  // تقرير شفافية سنوي
  transparency_report: true,
};
```

---

# الأمر 14: خطة الجودة والاختبار القاسي (QA)

> **المرجع:** أمر QA #14
> **الهدف:** 20+ سيناريو اختبار لا يرحم.

---

## خطة الاختبار الشاملة (25 سيناريو):

### 🔴 اختبارات سلبية (Negative Tests):

| # | السيناريو | المدخل | السلوك المتوقع |
|---|-----------|--------|---------------|
| 1 | وصف سخيف | "أريد موقعًا ورديًا للديناصورات" | يقبل ويولد موقعًا إبداعيًا بألوان وردية مع عناصر ديناصور — لا يرفض |
| 2 | وصف فارغ | "" (فارغ) | رسالة: "الرجاء وصف مشروعك لنتمكن من إنشاء موقعك" + توجيه لبروتوكول المحادثة |
| 3 | وصف طويل جدًا | 2000+ كلمة | يقتطع أهم المعلومات ويعرض ملخصًا للتأكيد |
| 4 | وصف بلغة غير مدعومة | نص بالصينية | رسالة: "حاليًا ندعم العربية والإنجليزية والفرنسية والإسبانية" |
| 5 | أحرف خاصة فقط | "!!!@@@###" | رسالة: "لم نتمكن من فهم الوصف. هل يمكنك إعادة صياغته؟" |
| 6 | محتوى هجومي/ضار | نص مسيء | كشف المحتوى + رفض + رسالة سياسة الاستخدام |
| 7 | حقن HTML/JS | `<script>alert('hack')</script>` | تنظيف تام (sanitize) + معاملة كنص عادي |

### 🟡 اختبارات التوافق (Compatibility):

| # | السيناريو | طريقة الاختبار | معيار النجاح |
|---|-----------|---------------|-------------|
| 8 | Chrome (آخر نسختين) | Puppeteer/Playwright | لا أخطاء بصرية أو وظيفية |
| 9 | Firefox (آخر نسختين) | Playwright | لا أخطاء بصرية أو وظيفية |
| 10 | Safari (macOS + iOS) | BrowserStack | لا أخطاء بصرية أو وظيفية |
| 11 | iOS Safari (iPhone 12+) | BrowserStack | تجاوب كامل، لمس يعمل |
| 12 | Android Chrome (Pixel 6+) | BrowserStack | تجاوب كامل، لمس يعمل |
| 13 | اتجاه RTL (العربية) | اختبار يدوي | كل العناصر منعكسة بشكل صحيح |
| 14 | نصوص عربية طويلة | نص 500+ كلمة في كل حقل | لا كسر في التصميم |

### 🟢 اختبارات الأداء (Performance):

| # | السيناريو | الحد المقبول | أداة القياس |
|---|-----------|-------------|-----------|
| 15 | زمن توليد الموقع | ≤ 90 ثانية | Timer داخلي |
| 16 | زمن تحميل الصفحة | ≤ 2.5 ثانية (LCP) | Lighthouse |
| 17 | حجم الحزمة الأولى | ≤ 200 KB | webpack-bundle-analyzer |
| 18 | استقرار التخطيط | CLS ≤ 0.1 | Lighthouse |
| 19 | تجاوز الزمن | إذا > 90 ثانية | رسالة: "التوليد يأخذ وقتًا أطول من المعتاد. إعادة المحاولة تلقائيًا..." |

### 🔒 اختبارات الأمان (Security):

| # | السيناريو | الاختبار | معيار النجاح |
|---|-----------|---------|-------------|
| 20 | XSS via description | حقن `<script>` في الوصف | تنظيف تام + عدم تنفيذ |
| 21 | SQL Injection | حقن `'; DROP TABLE users;` | Prisma parameterized queries تمنع |
| 22 | CSRF Protection | طلب POST بدون CSRF token | رفض 403 |
| 23 | Rate Limiting | 100 طلب / دقيقة من IP واحد | حد 10 طلبات/دقيقة |
| 24 | Auth Bypass | وصول لـ /dashboard بدون تسجيل | إعادة توجيه لـ /login |
| 25 | File Upload Exploit | رفع ملف .php/.exe كصورة | رفض + رسالة: "نوع الملف غير مدعوم" |

---

# الأمر 15: شروط الخدمة وسياسة الخصوصية

> **المرجع:** أمر الخصوصية #15

---

## شروط الخدمة (نسخة مبسطة):

### 1. ملكية المحتوى
> المحتوى الذي يُنشئه الذكاء الاصطناعي بناءً على مدخلاتك يصبح **ملكك بالكامل** بعد الاشتراك في أي خطة مدفوعة. يمكنك استخدامه، تعديله، وتوزيعه بحرية.
>
> في الخطة المجانية: يظل المحتوى ملكك ولكن لا يمكنك إزالة علامة "GetYouSite" من أسفل الموقع.

### 2. استخدام البيانات
> نستخدم أوصاف المواقع (بشكل مجمّع ومجهول الهوية فقط) لتحسين جودة التوليد. **لن نشارك أبدًا** بيانات عملاء محددة أو معلومات شخصية مع أي طرف ثالث.
>
> **ما نجمعه:**
> - أنماط استخدام مجمّعة (أي مكونات يُعدّلها المستخدمون أكثر)
> - إحصائيات أداء (أوقات التحميل، معدلات الخطأ)
>
> **ما لا نجمعه أبدًا:**
> - المحتوى النصي الفعلي لمواقعك
> - بيانات عملائك أو زوار موقعك
> - معلومات الدفع (تُعالج عبر Stripe/PayPal مباشرة)

### 3. حدود المسؤولية
> GetYouSite أداة مساعدة لإنشاء المواقع. أنت تتحمل المسؤولية الكاملة عن:
> - محتوى موقعك النهائي والتزاماته القانونية
> - حقوق الملكية الفكرية للصور والنصوص التي ترفعها
> - الامتثال لقوانين بلدك (مثل GDPR إذا كنت في أوروبا)
>
> GetYouSite **ليست مسؤولة** عن:
> - أي أضرار ناتجة عن استخدام المحتوى المُولد
> - انقطاع الخدمة غير المتوقع (نضمن 99.9% وقت تشغيل)

### 4. سياسة الإلغاء والاسترداد
> - يمكنك إلغاء اشتراكك في أي وقت من لوحة التحكم
> - الإلغاء يسري في نهاية دورة الفوترة الحالية
> - **استرداد كامل** خلال أول 14 يومًا إذا لم تكن راضيًا
> - بعد 14 يومًا: لا استرداد للمبالغ المدفوعة (الدورة الحالية تبقى نشطة)
> - بعد الإلغاء: موقعك يبقى متاحًا لمدة 30 يومًا إضافية لتصدير بياناتك

---

*نهاية الأوامر 13-15 — التحليل والاختبار والخصوصية*
