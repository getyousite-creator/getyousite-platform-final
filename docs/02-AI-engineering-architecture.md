# الأمر 2: هندسة الذكاء الاصطناعي الأساسية — المساعد متعدد المراحل

> **المرجع:** أمر هندسة AI #2
> **الحالة:** مواصفات تقنية مفصلة

---

## السيناريو المرجعي

> **طلب العميل:** "أريد موقعًا لمطعم عائلي يقدم وجبات بحرية، مع قائمة طعام قابلة للتنزيل، ونموذج لحجز الطاولات، وصورة للطهاة، وألوان تذكرني بالبحر."

---

## المرحلة 1: التحليل والاستخلاص (Analysis & Extraction)

### الفصل بين المتطلبات:

```
┌─────────────────────────────────────────────────────────┐
│                    وصف العميل الخام                       │
│  "مطعم عائلي، وجبات بحرية، قائمة طعام، حجز طاولات،    │
│   صورة للطهاة، ألوان البحر"                              │
└──────────────────────┬──────────────────────────────────┘
                       │
           ┌───────────┴───────────┐
           │    محلل NLP (GPT-4o)   │
           └───────────┬───────────┘
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  وظيفية      │ │  جمالية      │ │  محتوى       │
│ Functional   │ │ Aesthetic    │ │ Content      │
├──────────────┤ ├──────────────┤ ├──────────────┤
│ • قائمة طعام │ │ • ألوان بحرية│ │ • نص ترحيبي  │
│   قابلة      │ │ • أجواء      │ │ • أوصاف      │
│   للتنزيل    │ │   عائلية     │ │   أطباق      │
│ • نموذج حجز  │ │ • صور        │ │ • عناوين     │
│   طاولات     │ │   بصرية      │ │   فرعية      │
│ • صفحة فريق  │ │   مشرقة      │ │ • معلومات    │
│   (الطهاة)   │ │              │ │   اتصال      │
└──────────────┘ └──────────────┘ └──────────────┘
```

### البرومبت الداخلي لمحلل NLP:

```typescript
const ANALYSIS_PROMPT = `
أنت محلل محترف لطلبات إنشاء المواقع.
حلل الوصف التالي واستخرج بيانات منظمة:

الوصف: "${userInput}"

أعد الإجابة بصيغة JSON مع الحقول التالية:
{
  "business_type": "نوع النشاط (restaurant, clinic, portfolio, etc.)",
  "business_subtype": "تخصص فرعي",
  "functional_requirements": ["قائمة بالوظائف المطلوبة"],
  "aesthetic_requirements": {
    "color_mood": "المزاج اللوني",
    "style": "النمط البصري",
    "image_priority": "high/medium/low"
  },
  "content_needs": ["أنواع المحتوى المطلوب"],
  "suggested_pages": ["الصفحات المقترحة مع ترتيبها"],
  "tone": "نغمة التواصل",
  "target_audience": "وصف الجمهور المستهدف"
}
`;
```

### المخرج من المرحلة 1:
```json
{
  "business_type": "restaurant",
  "business_subtype": "seafood-family",
  "functional_requirements": [
    "downloadable-menu",
    "reservation-form",
    "team-gallery"
  ],
  "aesthetic_requirements": {
    "color_mood": "ocean-calm",
    "style": "warm-modern",
    "image_priority": "high"
  },
  "content_needs": ["hero-text", "menu-descriptions", "chef-bios", "contact-info"],
  "suggested_pages": ["home", "menu", "about-team", "reservation", "contact"],
  "tone": "warm-welcoming-family",
  "target_audience": "families-25-55-local"
}
```

---

## المرحلة 2: التخطيط المعماري (Architectural Planning)

### تحديد الهيكل:

```
┌─────────────────────────────────────────────┐
│              خريطة الموقع                     │
│                                             │
│   ┌──────────┐                              │
│   │ الرئيسية  │ ← Hero + Features + CTA     │
│   └─────┬────┘                              │
│         │                                   │
│   ┌─────┼──────────┬──────────┬──────────┐  │
│   ▼     ▼          ▼          ▼          ▼  │
│ ┌────┐ ┌─────┐ ┌───────┐ ┌──────┐ ┌─────┐ │
│ │قائمة│ │الفريق│ │ الحجز  │ │اتصل │ │مدونة│ │
│ │طعام │ │     │ │       │ │ بنا │ │     │ │
│ └────┘ └─────┘ └───────┘ └──────┘ └─────┘ │
│                                             │
│ ← PDF   ← صور  ← نموذج  ← خريطة  ← مقالات│
│   تنزيل   الطهاة  تفاعلي   + هاتف    SEO  │
└─────────────────────────────────────────────┘
```

### خوارزمية اختيار المكونات:

```typescript
function selectComponents(analysis: SiteAnalysis): ComponentBlueprint[] {
  const components: ComponentBlueprint[] = [];

  // دائمًا: Hero Section
  components.push({
    type: 'hero',
    variant: analysis.image_priority === 'high' ? 'image-dominant' : 'text-dominant',
    content: { headline: '', subheadline: '', cta: '' }
  });

  // حسب النشاط: Features/Services
  if (analysis.business_type === 'restaurant') {
    components.push({ type: 'menu-showcase', variant: 'grid-with-prices' });
    components.push({ type: 'reservation-form', variant: 'date-time-guests' });
  }

  // حسب المتطلبات الوظيفية
  if (analysis.functional_requirements.includes('team-gallery')) {
    components.push({ type: 'team-grid', variant: 'photo-cards' });
  }

  // دائمًا: Contact + Footer
  components.push({ type: 'contact-section', variant: 'map-with-form' });
  components.push({ type: 'footer', variant: 'full-columns' });

  return components;
}
```

---

## المرحلة 3: التوليد (Generation)

### توليد النصوص (Content Generation):

```typescript
const CONTENT_GENERATION_PROMPT = `
أنت كاتب محتوى محترف متخصص في المواقع الإلكترونية.

معلومات المشروع:
- النوع: ${analysis.business_type}
- النغمة: ${analysis.tone}
- الجمهور: ${analysis.target_audience}
- اللغة: العربية الفصحى (سلسة وسهلة)

المطلوب:
1. عنوان رئيسي (Hero Title): جملة واحدة جذابة، 5-8 كلمات
2. عنوان فرعي: جملة واحدة توضيحية، 10-15 كلمة
3. فقرة ترحيبية: 80-120 كلمة، دافئة ومهنية
4. 3 نقاط قوة (Features): كل واحدة عنوان + وصف من سطرين
5. دعوة للإجراء (CTA): جملة تحفيزية + نص الزر

القواعد:
- ممنوع "لوريم إيبسوم" أو نصوص عامة
- كل كلمة يجب أن تكون ذات معنى وصلة بالنشاط
- استخدم لغة تنساب طبيعيًا وليست آلية
- لا تستخدم كلمات مبالغ فيها مثل "الأفضل في العالم"

مثال مخرج لمطعم بحري عائلي:
{
  "hero_title": "نكهات البحر على مائدتك",
  "hero_subtitle": "مطعم عائلي يقدم أشهى المأكولات البحرية الطازجة في أجواء دافئة",
  "welcome_paragraph": "في قلب المدينة، حيث يلتقي عبق البحر بدفء العائلة...",
  "features": [...],
  "cta_text": "احجز طاولتك الآن",
  "cta_button": "احجز الآن"
}
`;
```

### توليد Design Tokens:

```typescript
function generateDesignTokens(aesthetic: AestheticRequirements): DesignTokens {
  const palettes = {
    'ocean-calm': {
      primary: '#0077B6',      // أزرق بحري عميق
      secondary: '#00B4D8',    // فيروزي
      accent: '#90E0EF',       // أزرق فاتح
      background: '#CAF0F8',   // أبيض مزرق
      text: '#023E8A',         // أزرق داكن
      surface: '#FFFFFF',
    },
    'warm-earth': {
      primary: '#8B4513',
      secondary: '#D2691E',
      accent: '#DEB887',
      background: '#FFF8DC',
      text: '#3E2723',
      surface: '#FFFFFF',
    },
    // ... المزيد من اللوحات
  };

  return {
    colors: palettes[aesthetic.color_mood],
    typography: {
      headingFont: 'Noto Sans Arabic',
      bodyFont: 'Inter',
      headingSize: aesthetic.style === 'luxury' ? '3.5rem' : '2.5rem',
      bodySize: '1rem',
      lineHeight: 1.7,
    },
    spacing: {
      sectionPadding: aesthetic.image_priority === 'high' ? '80px' : '60px',
      componentGap: '24px',
    },
    borderRadius: aesthetic.style === 'modern' ? '12px' : '8px',
    shadows: aesthetic.style === 'luxury'
      ? '0 20px 60px rgba(0,0,0,0.15)'
      : '0 4px 20px rgba(0,0,0,0.08)',
  };
}
```

---

## المرحلة 4: التكامل (Integration)

### تجميع المكونات في موقع حقيقي:

```
┌─────────────────────────────────────────────────────────┐
│                  محرك التكامل (Assembler)                 │
│                                                         │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐               │
│  │Blueprint │  │ Content  │  │ Design   │               │
│  │ JSON     │ +│ Strings  │ +│ Tokens   │               │
│  └────┬────┘  └────┬─────┘  └────┬─────┘               │
│       │            │              │                      │
│       └────────────┼──────────────┘                      │
│                    ▼                                     │
│           ┌────────────────┐                             │
│           │ React Renderer │                             │
│           │ (SSR/SSG)      │                             │
│           └───────┬────────┘                             │
│                   │                                      │
│        ┌──────────┼──────────┐                           │
│        ▼          ▼          ▼                           │
│   ┌────────┐ ┌────────┐ ┌────────┐                      │
│   │HTML    │ │CSS     │ │JS      │                      │
│   │Files   │ │Styles  │ │Scripts │                      │
│   └────────┘ └────────┘ └────────┘                      │
│        │          │          │                           │
│        └──────────┼──────────┘                           │
│                   ▼                                      │
│           ┌────────────────┐                             │
│           │  Static Export  │                             │
│           │  أو Vercel      │                             │
│           │  Deploy         │                             │
│           └────────────────┘                             │
└─────────────────────────────────────────────────────────┘
```

### الكود الفعلي للتجميع:

```typescript
async function assembleSite(
  blueprint: SiteBlueprint,
  content: GeneratedContent,
  tokens: DesignTokens
): Promise<SiteOutput> {

  // 1. إنشاء هيكل الصفحات
  const pages = blueprint.pages.map(page => ({
    slug: page.slug,
    components: page.components.map(comp => ({
      ...comp,
      content: content[comp.contentKey],
      styles: applyTokens(comp.type, tokens),
    })),
  }));

  // 2. توليد CSS المخصص
  const customCSS = generateCSS(tokens);

  // 3. توليد التفاعلات
  const scripts = generateInteractions(blueprint.functional_requirements);

  // 4. تجميع الملفات النهائية
  return {
    pages,
    styles: customCSS,
    scripts,
    assets: await fetchAssets(blueprint),
    metadata: generateSEO(blueprint, content),
  };
}
```

---

## المخطط الانسيابي الكامل (Flowchart)

```
العميل يكتب وصفًا
        │
        ▼
   [تحليل NLP]
   هل الوصف كافٍ؟
   ├── لا ──→ [أسئلة توضيحية ذكية] ──→ العميل يجيب ──→ ↩
   └── نعم
        │
        ▼
   [استخلاص البيانات المنظمة]
   JSON Blueprint
        │
        ▼
   [اختيار القالب الأساسي]
   أقرب نمط تصميمي
        │
        ├──→ [توليد المحتوى] ──→ نصوص + عناوين + أوصاف
        ├──→ [توليد التصميم] ──→ ألوان + خطوط + تخطيط
        └──→ [اختيار المكونات] ──→ Hero + Features + CTA + ...
             │
             ▼
        [التكامل والتجميع]
        React Components + CSS + JS
             │
             ▼
        [عرض المراجعة]
        Preview على جميع الأجهزة
             │
             ├── "أريد تعديلات" ──→ [المحرر الذكي] ──→ ↩
             └── "مثالي، انشره!"
                  │
                  ▼
             [النشر الفوري]
             Vercel / نطاق فرعي / نطاق خاص
                  │
                  ▼
             🎉 الموقع مباشر!
```

---

*نهاية الأمر 2 — هندسة الذكاء الاصطناعي الأساسية*
