# الأمر 8: مواصفات المحرر البصري الذكي

> **المرجع:** أمر المحرر الذكي #8
> **المبدأ:** ليس مجرد "سحب وإفلات" — محرر يفهم ويقترح.

---

## واجهة المحرر — الوصف التصميمي

```
┌──────────────────────────────────────────────────────────────────┐
│ ┌────────────────────┐  GetYouSite Editor  ┌──────────────────┐ │
│ │ ↩️ تراجع │ ↪️ إعادة │                    │ 💾 حفظ │ 👁️ معاينة│ │
│ └────────────────────┘                     └──────────────────┘ │
├──────────────┬───────────────────────────────┬──────────────────┤
│              │                               │                  │
│  📱 الأجهزة  │   ┌───────────────────────┐   │  🎨 لوحة التحكم  │
│  ├ 💻 كمبيوتر│   │                       │   │                  │
│  ├ 📱 جوال   │   │   المعاينة المباشرة    │   │  ┌──────────────┐│
│  └ 📐 لوحي  │   │                       │   │  │ 📝 النص       ││
│              │   │  [انقر على أي عنصر    │   │  │ المحدد:       ││
│  🧩 المكونات │   │   لتعديله مباشرة]     │   │  │              ││
│  ├ + Hero    │   │                       │   │  │ "نكهات البحر  ││
│  ├ + Features│   │  ┌─────────────────┐   │   │  │  على مائدتك" ││
│  ├ + Gallery │   │  │  نكهات البحر    │   │   │  └──────────────┘│
│  ├ + Contact │   │  │  على مائدتك     │🔵 │   │                  │
│  ├ + Team    │   │  │                 │   │   │  🖌️ التنسيق:     │
│  ├ + CTA     │   │  │  [احجز الآن]    │   │   │  حجم: [  28px  ] │
│  ├ + FAQ     │   │  └─────────────────┘   │   │  لون: [#0077B6 ] │
│  └ + أكثر...│   │                       │   │  خط:  [Noto Sans]│
│              │   │  ┌─────────────────┐   │   │                  │
│  🤖 مساعد AI│   │  │  🦐 🐟 🦑      │   │   │  ┌──────────────┐│
│  ┌──────────┐│   │  │  قائمة الطعام   │   │   │  │ 🤖 اقتراح AI ││
│  │اكتب      ││   │  └─────────────────┘   │   │  │ "اجعل العنوان││
│  │طلبك...   ││   │                       │   │  │  أقصر"        ││
│  │          ││   │                       │   │  │ "أضف إيموجي" ││
│  └──────────┘│   └───────────────────────┘   │  └──────────────┘│
│              │                               │                  │
└──────────────┴───────────────────────────────┴──────────────────┘
```

---

## المواصفات التقنية التفصيلية:

### 1. التعديل المباشر (Direct Edit)

```typescript
interface DirectEditCapability {
  // النقر على أي نص → يصبح قابلاً للتعديل
  onElementClick: (element: HTMLElement) => {
    // تحديد العنصر بإطار أزرق متوهج
    highlightElement(element);
    // فتح المحرر النصي المباشر (contentEditable)
    enableInlineEdit(element);
    // عرض لوحة التحكم الذكية
    showPropertyPanel(element);
  };

  // حفظ تلقائي بعد كل تعديل
  autoSave: {
    debounceMs: 1500,
    strategy: 'optimistic', // يحفظ ثم يتحقق
  };
}
```

### 2. لوحة التحكم الذكية (Smart Property Panel)

```typescript
interface SmartPropertyPanel {
  // تتغير حسب العنصر المحدد
  contextual_properties: {
    text_element: ['content', 'font-size', 'font-weight', 'color', 'alignment'],
    image_element: ['src', 'alt', 'fit', 'border-radius', 'opacity'],
    section_element: ['background', 'padding', 'order', 'visibility'],
    button_element: ['text', 'url', 'color', 'size', 'shape'],
  };

  // اقتراحات AI فورية
  ai_suggestions: {
    for_text: ['اجعله أقصر', 'اجعله أكثر إقناعًا', 'ترجم للإنجليزية'],
    for_image: ['تحسين السطوع', 'اقتراح صورة بديلة', 'إزالة الخلفية'],
    for_section: ['إعادة ترتيب', 'تغيير التخطيط', 'نسخ المكون'],
  };
}
```

### 3. مساعد التعديل بالذكاء الاصطناعي (AI Tweaker)

```typescript
interface AITweaker {
  // أمثلة على الأوامر المدعومة:
  supported_commands: [
    'اجعل العنوان أكثر إثارة',        // → يعيد كتابة العنوان
    'غير لون الخلفية إلى أفتح',       // → يعدل CSS
    'أضف قسم آراء العملاء',           // → يضيف مكون جديد
    'احذف قسم الأسئلة الشائعة',       // → يخفي المكون
    'اجعل الموقع أكثر رسمية',          // → يعدل الألوان والخطوط والنغمة
    'أضف رقم الهاتف في الأعلى',       // → يعدل الـ Header
    'اجعل الصور أكبر',                // → يعدل حجم الشبكة
    'أضف زر واتساب',                  // → يضيف FloatingButton
  ];

  // آلية التنفيذ:
  processCommand: async (command: string, currentBlueprint: Blueprint) => {
    // 1. تحليل الأمر → فهم النية
    const intent = await analyzeIntent(command);

    // 2. تحديد المكونات المتأثرة
    const affected = findAffectedComponents(intent, currentBlueprint);

    // 3. إنشاء التعديلات
    const changes = await generateChanges(intent, affected);

    // 4. تطبيق مع الحفاظ على التناسق
    const result = applyWithConsistency(changes, currentBlueprint);

    // 5. عرض المعاينة قبل التأكيد
    return { preview: result, undo: () => rollback(currentBlueprint) };
  };
}
```

### 4. نظام التراجع اللامحدود (Unlimited Undo)

```typescript
interface UndoSystem {
  // كل تعديل يُخزن كـ "لقطة"
  history: {
    maxSnapshots: Infinity,       // بلا حدود
    storage: 'local + server',    // محفوظ حتى عند إغلاق المتصفح
    granularity: 'per-action',    // كل إجراء فردي
  };

  // واجهة التراجع:
  ui: {
    shortcut: 'Ctrl+Z / Cmd+Z',
    redo: 'Ctrl+Y / Cmd+Shift+Z',
    timeline: true,               // شريط زمني بصري للتعديلات
    named_snapshots: true,        // "حفظ هذه النسخة كـ 'قبل تغيير الألوان'"
  };
}
```

---

# الأمر 9: قواعد التوطين الصارمة — العربية كمواطن أول

> **المرجع:** أمر التوطين #9
> **القاعدة:** ممنوع الخليط العشوائي. ممنوع الترجمة الركيكة.

---

## القواعد الحديدية لمطوري الواجهة:

### القاعدة 1: جميع عناصر UI مترجمة ترجمة احترافية

```typescript
// ❌ خاطئ - ترجمة حرفية ركيكة
const BAD_TRANSLATIONS = {
  'submit': 'إرسال',           // ← صحيح لكن عام
  'back': 'خلف',              // ← خاطئ! "رجوع" أو "العودة"
  'cancel': 'إلغاء',          // ← صحيح
  'loading': 'تحميل...',      // ← ناقص! "جارٍ التحميل..."
  'sign_up': 'اشتراك',        // ← خاطئ! "إنشاء حساب"
  'dashboard': 'لوحة القيادة', // ← دقيقة ولكن "لوحة التحكم" أفضل
  'settings': 'إعدادات',      // ← ناقص! "الإعدادات"
  'delete': 'حذف',            // ← ناقص! "هل تريد حذف هذا العنصر؟"
};

// ✅ صحيح - ترجمة احترافية سياقية
const GOOD_TRANSLATIONS = {
  'submit': 'إرسال الطلب',
  'back': 'العودة',
  'cancel': 'إلغاء',
  'loading': 'جارٍ التحميل...',
  'sign_up': 'إنشاء حساب جديد',
  'dashboard': 'لوحة التحكم',
  'settings': 'الإعدادات',
  'delete_confirm': 'هل أنت متأكد من حذف هذا العنصر؟ لا يمكن التراجع عن هذا الإجراء.',
  'save_success': 'تم الحفظ بنجاح ✓',
  'save_error': 'حدث خطأ أثناء الحفظ. الرجاء المحاولة مرة أخرى.',
  'generating_site': 'جارٍ تصميم موقعك...',
  'generating_content': 'جارٍ كتابة المحتوى المناسب...',
  'generating_layout': 'جارٍ تنسيق التصميم البصري...',
  'site_ready': 'مبروك! موقعك جاهز للمراجعة 🎉',
};
```

### القاعدة 2: التبديل الكامل بين اللغات

```typescript
// الملفات المطلوبة:
// messages/ar.json ← كاملة 100%
// messages/en.json ← كاملة 100%
// messages/fr.json ← كاملة 100%
// messages/es.json ← كاملة 100%

// لا يُسمح أبدًا بـ:
// ❌ مفتاح موجود في en.json لكن مفقود في ar.json
// ❌ قيمة بالإنجليزية في ملف ar.json
// ❌ خليط لغتين في نفس الصفحة
```

### القاعدة 3: اختبار RTL الإلزامي

```css
/* كل مكون يجب أن يعمل بشكل مثالي مع: */
html[dir="rtl"] {
  /* ✅ الاختبارات المطلوبة: */
  /* 1. النصوص تبدأ من اليمين */
  /* 2. الأيقونات المتجهة (أسهم) تنعكس */
  /* 3. Flexbox و Grid يتبعان الاتجاه */
  /* 4. الهوامش والحشو (margin/padding) تنعكس */
  /* 5. النصوص الطويلة لا تكسر التصميم */
  /* 6. القوائم المنسدلة تفتح من الاتجاه الصحيح */
}
```

### القاعدة 4: رسائل الخطأ مرشدة وواضحة

```typescript
// ❌ ممنوع
'Error 500'
'Something went wrong'
'خطأ'
'فشل'

// ✅ المطلوب
const ERROR_MESSAGES = {
  network_error: {
    ar: 'يبدو أن الاتصال بالإنترنت ضعيف. الرجاء التحقق من اتصالك والمحاولة مرة أخرى.',
    en: 'It seems your internet connection is weak. Please check your connection and try again.',
  },
  generation_failed: {
    ar: 'حدث عطل أثناء إنشاء موقعك. تم تسجيل المشكلة وسنصلحها قريبًا. الرجاء المحاولة بعد 30 ثانية.',
    en: 'An issue occurred while creating your site. We\'ve logged it and will fix it soon. Please try again in 30 seconds.',
  },
  upload_too_large: {
    ar: 'حجم الملف كبير جدًا. الحد الأقصى المسموح هو 5 ميغابايت. الرجاء تقليل حجم الصورة.',
    en: 'File is too large. Maximum allowed size is 5MB. Please reduce the image size.',
  },
  session_expired: {
    ar: 'انتهت صلاحية جلستك لأسباب أمنية. الرجاء تسجيل الدخول مرة أخرى.',
    en: 'Your session has expired for security reasons. Please log in again.',
  },
};
```

---

*نهاية الأمرين 8 و 9 — المحرر الذكي والتوطين*
